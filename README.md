## Trade Broadcast Server

Sample server in Akka/Netty  that gets quote data from upstream and broadcasts it to multiple downstream clients

Use the following command in your tty: telnet localhost 5556